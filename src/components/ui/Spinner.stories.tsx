import { Spinner } from "./Spinner";
import { Meta } from "@storybook/react/types-6-0";

export default {
  component: Spinner,
  title: "Spinner",
} as Meta;

export const Default = () => <Spinner />;
